# Core
torch
torchvision
numpy
Pillow
opencv-python
scipy
tqdm
requests

# Model loading
huggingface_hub
safetensors

# 3D / PLY
plyfile
trimesh
open3d>=0.18.0
ninja

# Config / utils
omegaconf
einops
jaxtyping
typeguard
packaging

# COLMAP export (optional)
# pycolmap==3.10.0

# Gaussian Splatting (required for GS output and native HY-World trainer)
# Do not install PyPI/upstream gsplat here. HY-World worldgen requires the
# vendored fork at hyworld2/worldgen/third_party/gsplat_maskgaussian, installed
# by install.py / scripts/pipinstall.bat / the ComfyUI installer node.

# HY-World native GS scene trainer (optional WorldMirror V2 checkbox)
tyro
viser
nerfview
splines
imagesize
torchmetrics
tensorboard
pytorch-msssim
msgspec
shtab
yourdfpy

# CPU offloading (optional, for model_cpu_offload)
accelerate

# PyTorch3D (required for WorldStereo point rendering) - install manually:
# pip install "git+https://github.com/facebookresearch/pytorch3d.git@stable"

# MoGe depth estimator (required for WorldStereo)
git+https://github.com/microsoft/MoGe.git@0286b495230a074aadf1c76cc5c679e943e5d1c6

# WorldStereo
diffusers>=0.36.0
transformers>=4.40.0
bitsandbytes>=0.43.0
optimum-quanto>=0.2.4
torchao
imageio[ffmpeg]
kornia

# Sky segmentation ONNX fallback (optional)
# onnxruntime

# FP8 quantization requires a compatible GPU

# Video input (optional)
# moviepy==1.0.3
